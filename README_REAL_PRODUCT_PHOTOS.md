# Real Product Photos Restored

Only the supplied product images from `products.zip` were copied back into `assets/products/`. No substitute or generated product image was added. Product catalogue selection and data were otherwise left unchanged.
