# Software Tools Marketplace Production Release

Cumulative baseline. Public users can browse approved adverts without login. Registered sellers can submit adverts. Admin can add, approve, edit, archive, restore and remove adverts, manage sellers and view finance/tax operations.

## Production environment still required
Set real server secrets, persistent data/storage, live email and PayFast only after sandbox acceptance. Do not commit `.env`.
