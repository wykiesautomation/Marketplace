function parseCookies(req,res,next){req.cookies={};String(req.headers.cookie||'').split(';').forEach(p=>{const i=p.indexOf('=');if(i>0){try{req.cookies[p.slice(0,i).trim()]=decodeURIComponent(p.slice(i+1))}catch(_){}}});next()}
function setSecurityHeaders(req,res,next){res.setHeader('X-Content-Type-Options','nosniff');res.setHeader('X-Frame-Options','DENY');res.setHeader('Referrer-Policy','strict-origin-when-cross-origin');res.setHeader('Permissions-Policy','camera=(), microphone=(), geolocation=()');res.setHeader('Cross-Origin-Opener-Policy','same-origin');res.setHeader('Content-Security-Policy',"default-src 'self'; img-src 'self' data:; style-src 'self' 'unsafe-inline'; script-src 'self' 'unsafe-inline'; connect-src 'self'; form-action 'self' https://www.payfast.co.za https://sandbox.payfast.co.za; frame-ancestors 'none'; base-uri 'self'");if(process.env.NODE_ENV==='production')res.setHeader('Strict-Transport-Security','max-age=31536000; includeSubDomains');next()}
function rateLimit(){const bucket=new Map();return(req,res,next)=>{const k=(req.ip||'local')+':'+req.path,now=Date.now(),cur=bucket.get(k)||{t:now,c:0};if(now-cur.t>60000){cur.t=now;cur.c=0}cur.c++;bucket.set(k,cur);const limit=req.path.includes('/login')?10:180;if(cur.c>limit)return res.status(429).json({error:'Too many requests. Please try again shortly.'});next()}}
function requireSameOrigin(req,res,next){if(['GET','HEAD','OPTIONS'].includes(req.method))return next();if(req.path.includes('/payfast/notify'))return next();const origin=req.headers.origin;if(!origin)return next();try{if(new URL(origin).host!==req.headers.host)return res.status(403).json({error:'Invalid request origin'})}catch(_){return res.status(403).json({error:'Invalid request origin'})}next()}
module.exports={parseCookies,setSecurityHeaders,rateLimit,requireSameOrigin};
