const fs=require('fs');const db=JSON.parse(fs.readFileSync('data/db.json','utf8'));fs.mkdirSync('exports',{recursive:true});const rows=db.payoutLedger||[];const csv=['id,sellerId,orderRef,grossAmount,platformFee,sellerNet,status'].concat(rows.map(x=>[x.id,x.sellerId,x.orderRef,x.grossAmount,x.platformFee,x.sellerNet,x.status].join(','))).join('\n');const f='exports/payout-ledger-'+Date.now()+'.csv';fs.writeFileSync(f,csv);console.log(JSON.stringify({ok:true,file:f,rows:rows.length},null,2));
